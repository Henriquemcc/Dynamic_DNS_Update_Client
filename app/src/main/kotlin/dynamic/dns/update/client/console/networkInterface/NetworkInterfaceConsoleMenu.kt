package dynamic.dns.update.client.console

import dynamic.dns.update.client.console.common.menu.ConsoleButton
import dynamic.dns.update.client.console.common.menu.StaticConsoleMenu
import dynamic.dns.update.client.model.Host

/**
 * Console menu which allows the user to manage network interfaces.
 */
/*
fun networkInterfaceConsoleMenu(host: Host) {
    /**
     * Initialization block.
     */
    StaticConsoleMenu("Network interface menu", listOf(
        ConsoleButton("Show allowed network interfaces") { ListAllowedNetworkInterface(host) },
        ConsoleButton("Add allowed network interface") { AddAllowedNetworkInterface(host) },
        ConsoleButton("Remove allowed network interface") { DeleteAllowedNetworkInterface(host) }
    )
    )
}
 */